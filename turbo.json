{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "db:generate": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build", "^db:generate"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "dependsOn": ["^db:generate"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "type-check": {
      "dependsOn": ["build", "^build", "^db:generate"]
    },
    "test": {
      "dependsOn": ["^build", "^db:generate"]
    },
    "format:check": {
      "cache": false
    },
    "pr-check": {
      "dependsOn": ["format:check", "lint", "type-check", "build", "test"]
    },
    "clean": {
      "cache": false
    }
  }
}
